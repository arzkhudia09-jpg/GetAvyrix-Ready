services:
  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    working_dir: /app
    volumes:
      - ./backend/app:/app/app
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=
      - CORS_ORIGINS=http://localhost
